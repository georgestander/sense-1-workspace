export const WORKSPACE_SELECT_COLUMNS = `id,
        profile_id,
        scope_id,
        root_path,
        display_name,
        registered_at,
        last_active_at,
        session_count,
        metadata`;

export const SESSION_SELECT_COLUMNS = `id,
        profile_id,
        scope_id,
        actor_id,
        codex_thread_id,
        workspace_id,
        title,
        model,
        effort,
        status,
        started_at,
        ended_at,
        summary,
        metadata`;

export const PLAN_SELECT_COLUMNS = `id,
        session_id,
        profile_id,
        scope_id,
        actor_id,
        status,
        request_summary,
        assumptions,
        intended_actions,
        affected_locations,
        approval_status,
        approved_by_actor_id,
        approved_at,
        rejected_by_actor_id,
        rejected_at,
        created_at,
        updated_at,
        metadata`;

export const EVENT_SELECT_COLUMNS = `id,
        ts,
        actor_id,
        scope_id,
        verb,
        subject_type,
        subject_id,
        before_state,
        after_state,
        detail,
        engine_turn_id,
        engine_item_id,
        source_event_ids,
        causation_id,
        correlation_id,
        session_id,
        profile_id`;

export const OBJECT_REF_SELECT_COLUMNS = `id,
        session_id,
        ref_type,
        ref_path,
        ref_id,
        action,
        ts,
        metadata`;

export const QUESTION_SELECT_COLUMNS = `id,
        profile_id,
        scope_id,
        session_id,
        actor_id,
        codex_thread_id,
        engine_turn_id,
        request_id,
        prompt,
        status,
        answer_text,
        asked_at,
        answered_at,
        target_kind,
        target_id,
        target_snapshot,
        metadata`;
